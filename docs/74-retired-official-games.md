# 官方游戏移除记录

2026-09-06 按产品要求删除虫虫攀枝（bug-climb / lane-climb）和苔径迷庭（maze）。

- 移除官方登记、模板映射、推断入口、专用运行时、关卡配置、图片生成配置、模板资源和专用示例；更新审核脚本与测试名单。
- 删除当前数据库两款官方项目及虫虫早期开发副本，关联版本、发布、构建和游玩数据通过外键级联清理；删除固定游戏初始化标记。
- 依据数据库核对的构建和版本 ID 清理两个产物目录中的对应文件。永久批量删除被安全策略拦截、系统回收站不可用，因此专用资源和历史产物已移到项目外恢复目录 D:/codex-retired-games/2026-09-06，不再由平台或试玩服务提供。这不是安全擦除；不自动清空恢复目录。
- 纸境旧项目的五份 3D popup 历史合同借用了 maze 模板标识，仅将其标识兼容迁移为 generated；其作品、版本及游戏产物保留。不得按 maze 字段批量删除无关项目。
- 保留通用寻路、路线探索、换道和收集机制知识与组合能力；它们不再关联这两款官方模板。历史跨游戏研究记录只作为历史证据，不是当前上架或模板清单。
- 大厅现有 15 款，登记顺序连续；新请求使用 maze 模板必须被 schema 拒绝，已删除目录不得恢复。守卫见 src/shared/official-games/registry.test.ts。

数据库删除未提供应用内撤销；受版本控制的源码可由 Git 历史恢复，移出的资源可从上述恢复目录找回。

## 后续追加：删除纸境

同日用户追加删除纸境（paper-popup / popup-rotate-3d / threeMode=popup）。此前保留纸境的说明仅代表前一次删除时的范围，现已被本次要求取代。

- 删除纸境项目 398f8c09-e7af-43ee-bb80-2534c06e9129 及级联关联数据，移出数据库核对的构建/版本产物与专用图片、概念稿、试玩副本。
- 删除登记、模板、规则内核、关卡库、专用 PlayCanvas 运行时、生成分支和专用探针。合同及分析器不再接受 popup 模式；相关回归名单和维护脚本同步调整。
- PlayCanvas 通用引擎保留。引擎示例原本共用的两张项目自有贴图迁入 examples/engine-playcanvas-demo/assets，解除对已删除游戏的依赖；原哈希不变。
- 专用资源及产物沿用项目外恢复目录，不做永久擦除。纸境此前不在大厅，大厅仍为 15 款。
