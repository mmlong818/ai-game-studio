# 版本审核历史呈现

2026-09-07

- 每个版本的专业详情增加默认折叠的历史入口，仅展开时查询，收起或切换时中止旧请求。
- 显示审核顺序、前后结论、时间和原始依据；明确人工记录身份未验证。
- 空历史不等于通过或未通过，也不能推断旧版本从未审核；读取失败单独显示错误，只读重试不提交审核、不触发制作。
- 客户端校验响应结构和项目/版本归属；文本转义渲染，审核时间变化时重新读取。
- 类型检查通过，84 文件、364 项前端测试通过。新增默认不查询、空历史、读取失败与重试、文本安全呈现和复核后刷新测试。
- 真实浏览器经原生控件脚本点击展开，读取现有项目空历史并截图确认；浏览器工具坐标点击未成功展开，未将该对照结果称为完整人工交互验收。没有对真实项目录入审核或发布。
- 权限隔离、不可篡改存证和自动美术评审仍不在本批范围内。
