# 交接记录：2026-09-08 Claude → Codex

本文只记录交接时刻的状态与未完成事项，不是规范。当前状态以 `docs/README.md`、根 `README.md`、`07-implementation-status.md` 为准。

## 代码状态

- 分支 `feature/production-reliability`，提交 `142f203` 已推到 `origin`，`main` 停在 `fc2b4fb`，**尚未合并**。
- 工作树里唯一未跟踪的是 `public/generated/`（1.7 MB，旧 vite 生图插件留下的图片，接手前就存在，未纳入版本库）。
- 本轮改动的说明在 `docs/97`–`100`；`docs/92`、`docs/94` 已相应改写。

## 本轮做了什么（详见对应文档）

1. `97`：图先行链路首次真实制作。蓝图声明的局内主体此前只进计划不进生成（每次生成游戏必然中断），已修；运行时合同补上验收的隐性要求；三次真实制作后交付《海边捡贝壳》。
2. `98`：Claude CLI `--json-schema` 需要纠错轮，`--max-turns 1` 导致方案生成成批失败；改为 3 轮、撤回被拒增量、错误原文透出。
3. `99`：制作步骤流式进展（`build_steps.live_excerpt`）、制作页改成工作台布局、失败后手动重新制作。
4. `100`：质量问题自动修正到交付（默认 6 轮，`STUDIO_MAX_REPAIR_ROUNDS`），远端故障仍立即停止；失败后帮助面板可收起、不再挡住游戏。

## 本机运行环境

- 服务已停止。PostgreSQL 容器 `ai-game-studio-postgres-1` 仍在运行（`npm run db:down` 可停），数据在卷里。
- 这台机器上 Docker Desktop 启动后 Hyper-V 保留 TCP 4199–4801，`4311/4312/4313` 绑定失败；本轮 API 跑在 `PORT=5312 GAME_PORT=5313`，工作台由 API 直接托管 `dist-web`（`npx vite build` 已是最新）。恢复标准端口需管理员执行 `net stop winnat` → `netsh int ipv4 add excludedportrange protocol=tcp startport=4311 numberofports=3 store=persistent` → `net start winnat`。
- 文本模型走 `STUDIO_TEXT_PROVIDER=claude-cli`（`.env.local`），图片走 OpenAI；两者密钥都在 `.env.local`。
- 部署包：`output/deploy-transfer-20260908/`（全库 `pg_dump` + 753 MB 产物 tar + `RESTORE.md` + 校验值），尚未传到部署机。

## 测试证据（交接时单次运行）

`npm run typecheck` 通过；`npm run test:platform` 403 项通过；`tests/game-generator.test.ts`、`tests/image-generator.test.ts`、`tests/design-contract.test.ts`、`tests/studio-repository.test.ts`、`tests/generated-source.test.ts` 通过。`npm run test:legacy` 全量在本轮早期跑过 287 项通过，之后改动只重跑了相关文件。`npm run test:browsers` 本轮未跑。

## 未完成 / 需要接手判断

- 已交付的旧版本（两个贝壳游戏、`31fa3714`）仍带旧的失败后帮助面板，重新制作才会更新。
- 失败面板在失败瞬间仍会视觉上盖住结算面板底部（点击可穿透），是否改到顶部或结算出现时自动收起，待定。
- 默认 6 轮自动修正是否真的提高交付率，没有统计，需看后续真实制作记录。
- 创作页反复点"重新生成方案"仍会并行发起多个 CLI 调用、旧请求不取消。
- 主美复核仍需 `STUDIO_REVIEW_TOKEN` 审核端提交，`ed624372` 与 `c1ffffe1` 的最新版本处于待复核、未发布。
- 桌面画幅下生成游戏页面偏窄留白多，验收只查横向溢出。
- 生成游戏的趣味性没有真人试玩证据。
