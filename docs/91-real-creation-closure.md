# 真实创作链路修复记录

2026-09-07，持续验收中。不可把本页理解为所有类型或上线条件已完成。

## 真实样本

- 项目：`22ae3969-3760-4ef0-b904-d361a16b7d1c`，七关花园记忆翻牌，6至12对，没有失败和倒计时。
- 原任务只提交一次；返回未改动方案不重复策划，刷新不重复创建，均通过浏览器请求记录核对。
- 原构建`6de4de36-4091-4aab-aca9-0926f24fbdf3`失败。静态预检不识别getter/var访问平台接口，误判并多消耗一次生成。
- 在原代码上修复并离线验收后，对同项目明确提交一次恢复构建`6cca5bc3-30c1-4d84-9e2d-5885bb020c4d`。代码最初被复用，规则审核仍触发一次定向修正，随后生成图片和完成版本交付。不是零额外模型调用，也不是自动重放旧回执。
- 新版本为v2，服务端自动检查通过，人工美术审核pending，未发布。浏览器已实际点开制作页内嵌游戏并看到首进教学。
- 追加的自然通关检查在390px手机和1366px桌面上，读取牌面身份制定解法并正常点击完成首关。没有用forceWin。只覆盖首关，不宣称七关全部自然通关。

后续连续正常输入已在手机和桌面各完成七关，12至24张牌按关递增，最后只有重游入口；发现并修复了手机结算裁切。最终CSS修复版本为 `6855393f-34f0-4da6-8b47-f72bb52a9726`，未发布。脚本逐字未改，图片按原溯源复用。局部修复三次任务各执行一次真实规则审核（共3次），没有代码模型或图像模型调用：第一次意外复用原代码未带入补丁；第二次解决裁切但结算排在棋盘后；第三次使结算直接替代棋盘展示。三次均保留版本记录，不伪造为一次成功。

## 平台修复

1. 无失败/无限确认方案贯穿设计合同与最终验收。新增专用抽样证据类型，不将失败帮助或有限关卡的不适用项计成通过。旧官方模板保留原检查。
2. 静态接线允许合法的getter及var别名；仍检查平台调用存在，并由浏览器证明动作信号。静态规则不宣称完整语义分析。
3. 呼吸动画通过真实指针位置点击，保留命中和遮挡检查；不靠关闭动画或force点击过检。
4. 复演等待真实动作完成，不以30毫秒替代游戏反馈周期；教学动作打开帮助后通过可见关闭按钮结束交互。
5. 平台教学层预留滚动空间，发出`forge:onboarding-change`，供任意关卡重看时恢复引导状态。
6. 失败后提供只读项目入口；制作进行中不增加另建/重试入口。完成态压缩标题和容器层级，让试玩更靠近首屏，应用导航保留。
7. 后续构建将每轮产物拒绝、基础设施错误和规则审核缺项写入`_studio/GENERATION_ATTEMPTS.jsonl`，避免进度文本覆盖修复依据。此次已运行请求发生在补丁部署前，不回填推测的审核原因。
8. 生成游戏图片增加项目/模型/画幅/运行时/完整提示词绑定的分组检查点；封面成功但背景失败时，后续仅补失败组。缓存验证PNG头与SHA-256，并保留原生成时间；缓存不是美术审核，也不免除资源接入验收。
9. 成功版本附带的平台runtime-inspector不再阻止代码复原；只剥离精确的平台注入标签，其他外链继续拒绝。
10. 记忆翻牌正常首关及最终关结算布局检查禁止按钮被overflow裁切；最终关探针只准备布局场景，不冒充自然通关。另有双端七关连续正常输入的独立证据。

## 验证

平台84文件373项通过；旧游戏与服务端全量276项通过；完整三浏览器70项通过、2项按既定范围跳过（非Chromium不重复跑空当接龙完整求解）。最后的源代码复用、结算检查和生成提示变更另有37项受影响回归通过；类型检查和生产构建通过。PostgreSQL隔离并发集成测试通过。不可把局部玩法覆盖外推为任意生成游戏均已完整通关。

## 仍需继续

审核凭据校验与事务内发布拦截已实现，配置与权限边界见[制作安全与审核权限](92-production-safety-and-review.md)。完整自然玩法能力覆盖、金额级预算及外部结果对账、更多真实修改/失败恢复路径、全量回归最终结果与部署收尾仍待完成。单主任务所有权不等于多活高可用；进行中未知费用调用仍不自动重试。
