# 《无限三消》官方固定游戏晋升记录

## 来源与范围

- 原始用户项目：`93620a9e-9916-4d4b-9301-e114ea1aa3a4`。
- 晋升的不可变成功版本：`58151dd2-9217-4526-b2a0-79540ee0109a`。
- 固定游戏目录：`fixtures/endless-match3/`；官方登记：`src/shared/official-games/endless-match3.ts`。
- 用户项目、production 回执与历史构建记录继续保留；官方固定游戏以该成功版本制作可移植副本，不依赖临时目录或某个数据库已有记录。
- 官方展示名从原项目的“自由创想”改为“无限三消”。静态副本只在 `index.html` 的 `<title>`、主标题和开始面板标题改了这三处文字；玩法脚本与样式未改。因此副本不是成功版本的字节级副本，随附原浏览器与设计审核回执仅作为来源版本证据，不表示改名后文件哈希再次经过模型审核。

## 已确认玩法

作品是 9:16 无限三消：拖动或点选交换相邻方块，三个及以上相同方块消除，上方方块下落补齐并可继续连锁；没有可消交换时自动洗牌。局内只有得分与重新开始，没有通关、失败、倒计时、生命、等级、教学流程或关卡扩展。

全部游戏画面由 Canvas / CSS 程序绘制，没有局内图片外呼。封面是成功版本浏览器验收时的真实运行截图，来源见 `fixtures/endless-match3/_studio/ART_PROVENANCE.md`。

## 晋升验收

成功版本的浏览器报告覆盖 360×640、390×844、1366×900，验证正常交换、消除、下落补齐、重开后再次操作、无限状态与禁止失败状态；设计验收报告绑定 `ENDLESS-SAMPLED` 与 `NO-FAILURE-SAMPLED`。晋升后仍需通过官方登记表守卫、固定游戏幂等注册、`GET /api/games` 大厅列表和实际稳定网址试玩。

2026-09-15 的本地晋升验收结果：

- PostgreSQL 启动同步创建官方项目 `01114a7e-2041-4753-b07b-67ef7d256bef`；服务重启后仍是同一项目，`GET /api/games` 中只有一条 `fixtureKind=endless-match3`，标题为“无限三消”，发布状态为 `live`。
- 稳定入口是 `http://127.0.0.1:4313/play/endless-match3/`；390×844 浏览器实测第一次合法交换从 0 分变为 55 分，重新开始归零后再次合法交换变为 30 分，状态始终为 `playing`，控制台与页面错误列表为空。
- 大厅截图：`.tmp/endless-match3-official-lobby.png`；玩法截图：`.tmp/endless-match3-official-play.png`。截图已人工检查，竖屏棋盘完整，没有挤压或遮挡。
- 官方登记表测试 11/11、固定目录与幂等同步测试 2/2、应用与服务端类型检查、生产构建均通过。本次晋升和改名没有调用文字、图片或审核模型。
