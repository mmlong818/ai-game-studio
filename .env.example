# 复制为 .env.local 后填写；该文件不会被 Git 提交。
OPENAI_API_KEY=
# 本机有系统代理（HTTP_PROXY/HTTPS_PROXY）时打开，否则 Node 的 fetch 不走代理，AI 调用会超时。
# NODE_USE_ENV_PROXY=1

# 文本模型改走本机 Claude Code CLI 订阅额度（策划、规则审核、代码生成）；图片仍需 OPENAI_API_KEY。
# STUDIO_TEXT_PROVIDER=claude-cli
# STUDIO_CLAUDE_MODEL=opus
# STUDIO_CLAUDE_CLI=claude
