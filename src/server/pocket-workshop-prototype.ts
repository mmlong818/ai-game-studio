import { mkdirSync, writeFileSync } from "node:fs";
import { join, relative } from "node:path";
import { chromium, type Browser } from "playwright";
import { POCKET_WORKSHOP_ASSET_REQUIREMENTS, POCKET_WORKSHOP_DESIGN_CONTRACT } from "../shared/game-design-knowledge/pocket-workshop-contract.js";
import { POCKET_WORKSHOP_ASSISTANCE, POCKET_WORKSHOP_LEVELS, POCKET_WORKSHOP_ONBOARDING } from "../shared/game-design-knowledge/pocket-workshop-golden.js";
import type { DesignResearchBrowserRunInput, DesignResearchProbeRunInput } from "./design-knowledge-evidence.js";
import { closeServer, requireBrowserExecutable, startArtifactServer } from "./browser-quality.js";

const json = (value: unknown) => JSON.stringify(value).replaceAll("<", "\\u003c");

export function generatePocketWorkshopPrototype(root: string) {
  mkdirSync(join(root, "_studio"), { recursive: true });
  writeFileSync(join(root, "_studio", "GAME_DESIGN_CONTRACT.json"), `${JSON.stringify(POCKET_WORKSHOP_DESIGN_CONTRACT, null, 2)}\n`, "utf8");
  writeFileSync(join(root, "_studio", "ASSET_REQUIREMENTS.json"), `${JSON.stringify(POCKET_WORKSHOP_ASSET_REQUIREMENTS, null, 2)}\n`, "utf8");
  writeFileSync(join(root, "_studio", "LEVELS.json"), `${JSON.stringify(POCKET_WORKSHOP_LEVELS, null, 2)}\n`, "utf8");
  writeFileSync(join(root, "_studio", "PROTOTYPE_STATUS.json"), `${JSON.stringify({ schemaVersion: "pocket-workshop-prototype-v1", status: "prototype-required", official: false, artStatus: "blocked-until-approved-original-assets", purpose: "mechanic-and-onboarding-validation" }, null, 2)}\n`, "utf8");
  writeFileSync(join(root, "index.html"), `<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>掌心工坊 · 玩法候选原型</title><link rel="icon" href="./favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="./styles.css"></head><body><main><header class="top"><div><small>GOLDEN CANDIDATE · 非正式模板</small><h1>掌心工坊</h1><p>摆好零件，让相邻回路一次点亮。</p></div><button id="replay-help" class="quiet">重看帮助</button></header><section class="progress" aria-label="关卡进度"><div id="level-list"></div></section><section class="game"><article><header class="level-head"><div><small id="chapter"></small><h2 id="level-name"></h2></div><div class="score"><span>当前 <b id="score">0</b></span><span>目标 <b id="target">0</b></span></div></header><div id="board" class="board" aria-label="维修盒格位"></div><div class="toolbar"><button id="rotate">旋转所选零件 <kbd>R</kbd></button><button id="undo">撤销</button><button id="clear">重新摆放</button></div></article><aside><section id="guide" class="guide" aria-live="polite"><small>新手帮助</small><strong id="guide-title"></strong><p id="guide-text"></p><button id="skip-help" class="quiet">跳过帮助</button></section><section><small>待装零件</small><div id="parts" class="parts"></div></section><section class="synergy"><small>协同预览</small><div id="preview" aria-live="polite"></div></section><button id="resolve" class="primary">试机</button><button id="hint" class="quiet">给我一点提示</button></aside></section><section id="result" class="result" aria-live="assertive" hidden><div><small>试机报告</small><h2 id="result-title"></h2><p id="result-summary"></p><ol id="breakdown"></ol></div><button id="next" class="primary">下一关</button></section><footer>程序化视觉仅用于验证玩法；原创美术、音频与资源审批仍是正式发布硬门槛。</footer></main><script src="./app.js"></script></body></html>`, "utf8");
  writeFileSync(join(root, "favicon.svg"), `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect width="32" height="32" rx="7" fill="#342b25"/><circle cx="16" cy="16" r="8" fill="#efb44f"/><path d="M16 5v5m0 12v5M5 16h5m12 0h5" stroke="#fffaf0" stroke-width="3"/></svg>`, "utf8");
  writeFileSync(join(root, "styles.css"), `:root{font-family:"Microsoft YaHei",system-ui,sans-serif;color:#342b25;background:#efe7d7;--ink:#342b25;--paper:#fffaf0;--copper:#a84d2f;--green:#34705b;--line:#bdae98;--cell:64px}*{box-sizing:border-box}[hidden]{display:none!important}body{margin:0;min-height:100dvh;background:radial-gradient(circle at 20% 0,#fff9e9 0,transparent 32%),linear-gradient(135deg,#e9dfcc,#f7f0e3)}button{min-width:44px;min-height:44px;border:1px solid #9f8e77;border-radius:10px;background:#f9f1df;color:var(--ink);font:700 14px inherit;cursor:pointer}button:focus-visible{outline:3px solid #176ea2;outline-offset:2px}button:disabled{opacity:.42;cursor:not-allowed}.primary{background:var(--copper);border-color:#87381f;color:white}.quiet{background:transparent}main{width:min(1120px,calc(100% - 28px));margin:auto;padding:28px 0}.top{display:flex;align-items:end;justify-content:space-between;padding-bottom:20px;border-bottom:2px solid var(--ink)}small{font:700 11px ui-monospace,monospace;letter-spacing:.08em;color:#796957}.top h1{margin:4px 0;font-size:clamp(38px,6vw,70px);line-height:.92;letter-spacing:-.06em}.top p{margin:10px 0 0;color:#6e6053}.progress{padding:15px 0;overflow-x:auto}.progress>div{display:flex;gap:7px}.level-button{flex:0 0 44px}.level-button[aria-current=true]{background:var(--ink);color:white}.game{display:grid;grid-template-columns:minmax(430px,1fr) 310px;border:1px solid var(--line);background:rgba(255,250,240,.8)}article{padding:24px}.level-head{display:flex;align-items:end;justify-content:space-between;margin-bottom:18px}.level-head h2{margin:4px 0 0;font-size:25px}.score{display:flex;gap:14px}.score span{display:grid;text-align:center;font-size:11px;color:#766657}.score b{font-size:22px;color:var(--ink)}.board{display:grid;width:min(100%,calc(var(--size)*var(--cell)));aspect-ratio:1;grid-template-columns:repeat(var(--size),1fr);gap:5px;margin:auto}.cell{position:relative;min-width:44px;min-height:44px;padding:2px;border-radius:8px;background:#e8dcc6}.cell.blocked{background:repeating-linear-gradient(45deg,#8f8271 0 5px,#b4a58f 5px 10px);cursor:not-allowed}.cell.occupied{border-color:#72513e;color:#241b16}.cell[data-kind=core]{background:#efb44f}.cell[data-kind=battery]{background:#6cb69c}.cell[data-kind=gear]{background:#8ca4bd}.cell[data-kind=coolant]{background:#72b9d0}.cell[data-kind=crystal]{background:#c28ccd}.cell[data-kind=relay]{background:#e78263}.cell[data-kind=shield]{background:#ada99f}.cell.synergy-cell{box-shadow:inset 0 0 0 3px #fff,0 0 0 2px var(--green)}.cell.hint-cell{animation:pulse 1s infinite}.cell span{display:block;overflow:hidden;font-size:10px;text-overflow:ellipsis}.toolbar{display:grid;grid-template-columns:2fr 1fr 1fr;gap:8px;margin-top:16px}kbd{padding:1px 5px;border:1px solid currentColor;border-radius:3px}aside{display:grid;align-content:start;gap:18px;padding:24px;border-left:1px solid var(--line);background:#f8eedc}aside section{display:grid;gap:8px}.guide{padding:14px;border-left:4px solid #d99d33;background:#fff7df}.guide strong{font-size:16px}.guide p{margin:0;color:#67594c;font-size:13px;line-height:1.55}.parts{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.part{display:grid;min-height:55px;padding:8px;text-align:left}.part span{font-size:11px;color:#77695b}.part.selected{border-color:var(--copper);box-shadow:inset 0 0 0 2px var(--copper)}.part.placed{background:#e2eadf}.synergy{min-height:100px}.synergy p{margin:0;color:#706052;font-size:12px;line-height:1.5}.synergy b{color:var(--green)}.result{display:flex;align-items:center;justify-content:space-between;gap:20px;margin-top:16px;padding:22px;border:1px solid var(--line);background:var(--paper)}.result h2{margin:5px 0}.result p{margin:0;color:#67584b}.result ol{margin:12px 0 0;padding-left:20px;font-size:13px}.result.fail{border-color:#b7583d}.result.win{border-color:#4c8a70}footer{padding:20px 0;color:#786a5c;font-size:11px}@keyframes pulse{50%{box-shadow:0 0 0 5px #d4872f}}@media(max-width:760px){:root{--cell:54px}main{width:min(100% - 16px,1120px);padding-top:16px}.top{align-items:start}.top h1{font-size:42px}.game{grid-template-columns:1fr}article{padding:14px}.level-head{align-items:start}.board{gap:4px}.toolbar{grid-template-columns:1fr 1fr}.toolbar button:first-child{grid-column:1/-1}aside{padding:16px;border-top:1px solid var(--line);border-left:0}.parts{grid-template-columns:repeat(3,1fr)}.result{align-items:stretch;flex-direction:column}.result button{width:100%}}`, "utf8");
  writeFileSync(join(root, "app.js"), `const levels=${json(POCKET_WORKSHOP_LEVELS)};const onboarding=${json(POCKET_WORKSHOP_ONBOARDING)};const assistance=${json(POCKET_WORKSHOP_ASSISTANCE)};const labels={core:"核心",battery:"电池",gear:"齿轮",coolant:"冷却器",crystal:"晶体",relay:"继电器",shield:"护板"};const synergy={core:{battery:5,gear:4,coolant:3,relay:3,shield:2},battery:{crystal:3,coolant:2,relay:2},gear:{crystal:2,relay:3},crystal:{relay:2},shield:{coolant:2}};const $=s=>document.querySelector(s);let state={level:0,placements:[],selected:null,rotation:0,history:[],failures:0,hint:null,guide:Number(localStorage.getItem("pocket-guide")||0)};let unlocked=Math.max(1,Number(localStorage.getItem("pocket-unlocked")||1));function cells(part,p){let out=part.cells.map(c=>[...c]);for(let t=0;t<((p.rotation%4)+4)%4;t++){out=out.map(([r,c])=>[c,-r]);const mr=Math.min(...out.map(x=>x[0])),mc=Math.min(...out.map(x=>x[1]));out=out.map(([r,c])=>[r-mr,c-mc])}return out.map(([r,c])=>[r+p.row,c+p.column])}function level(){return levels[state.level]}function validate(ps){const l=level(),blocked=new Set(l.blocked.map(x=>x.join(":"))),seen=new Set,occupied=new Map,errors=[];for(const p of ps){const part=l.parts.find(x=>x.id===p.partId);if(!part){errors.push("未知零件");continue}if(seen.has(p.partId)){errors.push("重复零件");continue}seen.add(p.partId);for(const [r,c] of cells(part,p)){const key=r+":"+c;if(r<0||c<0||r>=l.boardSize||c>=l.boardSize)errors.push("零件越出维修盒");else if(blocked.has(key))errors.push("损坏格不能放零件");else if(occupied.has(key))errors.push("零件发生重叠");else occupied.set(key,p.partId)}}return{valid:!errors.length,complete:!errors.length&&seen.size===l.parts.length,errors,occupied}}function pairValue(a,b){return synergy[a]?.[b]??synergy[b]?.[a]??0}function resolve(ps){const v=validate(ps);if(!v.valid)return{...v,score:0,won:false,activations:[]};const l=level(),pairs=new Set;for(const [key,owner] of v.occupied){const [r,c]=key.split(":").map(Number);for(const [dr,dc] of [[1,0],[0,1]]){const other=v.occupied.get((r+dr)+":"+(c+dc));if(other&&other!==owner)pairs.add([owner,other].sort().join("|"))}}const activations=[...pairs].map(key=>{const [left,right]=key.split("|");return{left,right,value:pairValue(l.parts.find(x=>x.id===left).kind,l.parts.find(x=>x.id===right).kind)}}).filter(x=>x.value>0).sort((a,b)=>b.value-a.value);const score=(v.complete?l.parts.length:ps.length)+activations.reduce((s,x)=>s+x.value,0);return{...v,score,won:v.complete&&score>=l.targetScore,activations}}function save(){localStorage.setItem("pocket-unlocked",String(unlocked));localStorage.setItem("pocket-guide",String(state.guide))}function render(){const l=level(),out=resolve(state.placements);document.body.dataset.level=String(l.number);document.body.dataset.gameState=out.won?"won":"playing";$("#chapter").textContent="第 "+l.tier+" 章 · "+l.chapter;$("#level-name").textContent=l.number+". "+l.name;$("#target").textContent=l.targetScore;$("#score").textContent=out.score;$("#level-list").replaceChildren(...levels.map((x,i)=>{const b=document.createElement("button");b.className="level-button";b.textContent=x.number;b.disabled=x.number>unlocked;b.setAttribute("aria-label","第 "+x.number+"关 "+x.name);b.setAttribute("aria-current",String(i===state.level));b.onclick=()=>load(i);return b}));const byCell=new Map;for(const p of state.placements){const part=l.parts.find(x=>x.id===p.partId);for(const cell of cells(part,p))byCell.set(cell.join(":"),part)}const activeCells=new Set(out.activations.flatMap(a=>[...state.placements.filter(p=>p.partId===a.left||p.partId===a.right).flatMap(p=>cells(l.parts.find(x=>x.id===p.partId),p).map(c=>c.join(":")))]));$("#board").style.setProperty("--size",l.boardSize);const blocked=new Set(l.blocked.map(x=>x.join(":")));$("#board").replaceChildren(...Array.from({length:l.boardSize*l.boardSize},(_,i)=>{const r=Math.floor(i/l.boardSize),c=i%l.boardSize,key=r+":"+c,part=byCell.get(key),b=document.createElement("button");b.className="cell"+(blocked.has(key)?" blocked":"")+(part?" occupied":"")+(activeCells.has(key)?" synergy-cell":"")+(state.hint&&state.hint.row===r&&state.hint.column===c?" hint-cell":"");b.disabled=blocked.has(key);b.dataset.row=String(r);b.dataset.column=String(c);if(part){b.dataset.kind=part.kind;b.innerHTML="<span>"+labels[part.kind]+"</span>"}else b.setAttribute("aria-label","空格 "+(r+1)+","+(c+1));b.onclick=()=>place(r,c);return b}));$("#parts").replaceChildren(...l.parts.map(part=>{const placed=state.placements.some(p=>p.partId===part.id),b=document.createElement("button");b.className="part"+(state.selected===part.id?" selected":"")+(placed?" placed":"");b.dataset.part=part.id;b.innerHTML="<b>"+labels[part.kind]+"</b><span>"+(part.cells.length>1?"双格 · 可旋转":"单格")+(placed?" · 已放":"")+"</span>";b.onclick=()=>select(part.id);return b}));$("#rotate").disabled=!state.selected;$("#undo").disabled=!state.history.length;$("#clear").disabled=!state.placements.length;$("#preview").innerHTML=out.activations.length?out.activations.map(a=>"<p><b>+"+a.value+"</b> "+name(a.left)+" × "+name(a.right)+"</p>").join(""):"<p>让零件正上、下、左、右共边即可形成协同。</p>";renderGuide()}function name(id){return labels[level().parts.find(x=>x.id===id)?.kind]||id}function select(id){state.selected=id;state.rotation=state.placements.find(p=>p.partId===id)?.rotation||0;if(state.guide===0&&id.startsWith("core")){state.guide=1;save()}render()}function place(row,column){if(!state.selected)return;const prior=state.placements,without=prior.filter(p=>p.partId!==state.selected),candidate=[...without,{partId:state.selected,row,column,rotation:state.rotation}];const v=validate(candidate);if(!v.valid){$("#preview").innerHTML="<p>这里放不下："+v.errors[0]+"。</p>";return}state.history.push(prior);state.placements=candidate;state.hint=null;const core=state.placements.find(p=>p.partId.startsWith("core")),battery=state.placements.find(p=>p.partId.startsWith("battery"));if(state.guide===1&&core&&battery&&resolve(state.placements).activations.some(a=>a.left.includes("battery")&&a.right.includes("core")||a.right.includes("battery")&&a.left.includes("core"))){state.guide=2;save()}render()}function rotate(){if(!state.selected)return;state.rotation=(state.rotation+1)%4;render()}function load(index){state={...state,level:index,placements:[],selected:null,rotation:0,history:[],failures:0,hint:null};$("#result").hidden=true;render()}function renderGuide(){const g=$("#guide");if(state.guide>=onboarding.length){g.hidden=true;return}g.hidden=false;$("#guide-title").textContent="步骤 "+(state.guide+1)+" / "+onboarding.length;$("#guide-text").textContent=onboarding[state.guide].instruction}function tryMachine(){const out=resolve(state.placements),box=$("#result");box.hidden=false;box.className="result "+(out.won?"win":"fail");$("#result-title").textContent=out.won?"回路点亮":"还差一点";$("#result-summary").textContent=!out.complete?"还有零件没有正确装入。":out.won?"得到 "+out.score+" 分，达到目标。":"得到 "+out.score+" 分，距离目标还差 "+(level().targetScore-out.score)+" 分。";$("#breakdown").replaceChildren(...out.activations.map(a=>{const li=document.createElement("li");li.textContent=name(a.left)+" × "+name(a.right)+"：+"+a.value;return li}));$("#next").hidden=!out.won;if(out.won){if(state.guide===2){state.guide=3;save()}unlocked=Math.max(unlocked,Math.min(20,level().number+1));save()}else state.failures++;render()}function hint(){const h=assistance[Math.min(Math.max(state.failures,1),3)-1];const missing=level().solution.find(p=>!state.placements.some(x=>x.partId===p.partId));state.hint=h.afterFailures>=3&&missing?missing:null;$("#preview").innerHTML="<p><b>提示：</b>"+h.message+"</p>";if(state.hint)render()}$("#rotate").onclick=rotate;$("#undo").onclick=()=>{state.placements=state.history.pop()||[];state.hint=null;render()};$("#clear").onclick=()=>{if(state.placements.length)state.history.push(state.placements);state.placements=[];state.hint=null;render()};$("#resolve").onclick=tryMachine;$("#hint").onclick=hint;$("#next").onclick=()=>load(Math.min(19,state.level+1));$("#skip-help").onclick=()=>{state.guide=onboarding.length;save();render()};$("#replay-help").onclick=()=>{state.guide=0;save();render()};addEventListener("keydown",e=>{if(e.key.toLowerCase()==="r")rotate()});window.__POCKET_WORKSHOP__={getState:()=>({...state,unlocked,resolution:resolve(state.placements)}),levels};render();`, "utf8");
  return { root, levelCount: POCKET_WORKSHOP_LEVELS.length };
}

export async function inspectPocketWorkshopPrototype(root: string, requiredSignals: string[], permanentUrl: string): Promise<{ probeRuns: DesignResearchProbeRunInput[]; browserRuns: DesignResearchBrowserRunInput[]; screenshots: string[] }> {
  const executablePath = requireBrowserExecutable("掌心工坊自动验收");
  const qualityRoot = join(root, "_studio", "quality"); mkdirSync(qualityRoot, { recursive: true });
  const signalFailures = new Map(requiredSignals.map((signal) => [signal, [] as string[]]));
  const browserRuns: DesignResearchBrowserRunInput[] = []; const screenshots: string[] = [];
  const { server, url } = await startArtifactServer(root); let browser: Browser | null = null;
  try {
    browser = await chromium.launch({ executablePath, headless: true });
    for (const viewport of [{ deviceClass: "desktop" as const, width: 1280, height: 800 }, { deviceClass: "mobile" as const, width: 390, height: 844 }]) {
      const page = await browser.newPage({ viewport: { width: viewport.width, height: viewport.height } });
      const failures: string[] = []; const runtimeErrors: string[] = []; const observed = new Set<string>();
      page.on("pageerror", (error) => runtimeErrors.push(error.message));
      page.on("console", (message) => { if (message.type() === "error") runtimeErrors.push(message.text()); });
      try {
        await page.goto(url, { waitUntil: "domcontentloaded", timeout: 10_000 });
        await page.locator("#resolve").click(); await page.locator("#resolve").click(); await page.locator("#resolve").click(); await page.locator("#hint").click();
        if ((await page.evaluate(() => (window as any).__POCKET_WORKSHOP__.getState().hint)) !== null) observed.add("failure-help-escalated");
        const solve = async (levelIndex: number) => {
          const level = POCKET_WORKSHOP_LEVELS[levelIndex]!;
          for (const placement of level.solution) {
            await page.locator(`[data-part="${placement.partId}"]`).click();
            if (placement.partId.startsWith("core") && levelIndex === 0 && Number(await page.evaluate(() => (window as any).__POCKET_WORKSHOP__.getState().guide)) >= 1) observed.add("part-inspected");
            for (let turn = 0; turn < placement.rotation; turn += 1) await page.locator("#rotate").click();
            if (placement.rotation > 0 && levelIndex === 8) observed.add("multi-cell-rotated");
            await page.locator(`[data-row="${placement.row}"][data-column="${placement.column}"]`).click();
          }
          if (levelIndex === 0 && Number(await page.evaluate(() => (window as any).__POCKET_WORKSHOP__.getState().guide)) >= 2) observed.add("synergy-previewed");
          await page.locator("#resolve").click();
          const result = await page.evaluate(() => (window as any).__POCKET_WORKSHOP__.getState());
          if (!result.resolution.won) failures.push(`第 ${level.number} 关作者解未通过可见控件完成`);
          if (levelIndex === 0 && result.guide >= 3 && result.resolution.activations.length > 0) observed.add("resolution-explained");
          return result.resolution;
        };
        const first = await solve(0); await page.locator("#resolve").click();
        const repeated = await page.evaluate(() => (window as any).__POCKET_WORKSHOP__.getState().resolution);
        if (JSON.stringify(first) !== JSON.stringify(repeated)) failures.push("相同布局重复试机结果不一致");
        await page.evaluate(() => localStorage.setItem("pocket-unlocked", "9")); await page.reload({ waitUntil: "domcontentloaded" }); await page.locator('[aria-label^="第 9关"]').click(); await solve(8);
        const layout = await page.evaluate(() => ({
          overflow: document.documentElement.scrollWidth - document.documentElement.clientWidth,
          main: Boolean(document.querySelector("main")), live: Boolean(document.querySelector("[aria-live]")),
          unlabeled: [...document.querySelectorAll("button")].filter((button) => (button as HTMLElement).offsetParent !== null && !button.textContent?.trim() && !button.getAttribute("aria-label")).length,
          undersized: [...document.querySelectorAll("button:not(:disabled)")].filter((button) => (button as HTMLElement).offsetParent !== null).filter((button) => { const box = button.getBoundingClientRect(); return box.width < 44 || box.height < 44; }).length,
        }));
        const accessibilityViolationCount = Number(!layout.main) + Number(!layout.live) + layout.unlabeled + layout.undersized;
        if (layout.overflow > 1) failures.push(`横向溢出 ${layout.overflow}px`);
        if (accessibilityViolationCount) failures.push(`基础无障碍检查发现 ${accessibilityViolationCount} 项问题`);
        for (const signal of requiredSignals) if (!observed.has(signal)) signalFailures.get(signal)?.push(`${viewport.deviceClass} 未观察到 ${signal}`);
        const screenshot = join(qualityRoot, `${viewport.deviceClass}-automatic.png`); await page.screenshot({ path: screenshot, fullPage: true }); screenshots.push(relative(root, screenshot).replaceAll("\\", "/"));
        browserRuns.push({ deviceClass: viewport.deviceClass, url: permanentUrl, viewport: `${viewport.width}x${viewport.height}`, status: failures.length || runtimeErrors.length ? "failed" : "passed", interactionCompleted: failures.length === 0 && requiredSignals.every((signal) => observed.has(signal)), consoleErrorCount: runtimeErrors.length, accessibilityViolationCount, observation: failures.length || runtimeErrors.length ? [...failures, ...runtimeErrors].join("；") : "通过可见控件完成教学、失败帮助、第一关确定性复演及第九关双格旋转，并核对触控尺寸和横向溢出。" });
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error); for (const signal of requiredSignals) signalFailures.get(signal)?.push(`${viewport.deviceClass} 验收中断`);
        browserRuns.push({ deviceClass: viewport.deviceClass, url: permanentUrl, viewport: `${viewport.width}x${viewport.height}`, status: "failed", interactionCompleted: false, consoleErrorCount: runtimeErrors.length, accessibilityViolationCount: 0, observation: message });
      } finally { await page.close(); }
    }
  } finally { await browser?.close(); await closeServer(server); }
  const probeRuns = requiredSignals.map((signalId) => ({ signalId, status: signalFailures.get(signalId)?.length ? "failed" as const : "passed" as const, observation: signalFailures.get(signalId)?.length ? signalFailures.get(signalId)!.join("；") : "桌面与手机均通过真实玩家控件观察到该设计信号。" }));
  writeFileSync(join(root, "_studio", "AUTOMATIC_EVALUATION.json"), `${JSON.stringify({ checkedAt: new Date().toISOString(), probeRuns, browserRuns, screenshots }, null, 2)}\n`, "utf8");
  return { probeRuns, browserRuns, screenshots };
}
